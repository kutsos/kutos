#!/bin/bash
# KutOS Installer launcher
# Bu dosya build.sh tarafından Go binary ile değiştirilir.
# Eğer hâlâ bu scripti görüyorsanız build.sh çalıştırılmamış demektir.
echo "Hata: kutos-installer henüz derlenmedi. Lütfen build.sh çalıştırın." >&2
exit 1
